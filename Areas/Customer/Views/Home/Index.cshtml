@{
    ViewData["Title"] = "Home Page";
}

@using OnLineShop.Models;
@using X.PagedList.Mvc.Core;
@using X.PagedList.Web.Common;
@model X.PagedList.IPagedList<Products>

<br />
<br />
<div class="row">
    @foreach (var product in Model)
    {
        <div class="col-4">
            <div class="card mb-4">
                <div class="card-header">
                    <h4 class="my-0 font-weight-normal">
                        <label style="font-size: 23px; color: steelblue;margin-bottom:10px">@product.Name</label>
                    </h4>
                    <img src="/@product.Image" alt="card image" class="card-img-bottom" style="height:300px"/>
                </div>
                <div class="card-header">
                    <div class="d-flex align-content-between align-items-center">
                        <div class="btn-group">
                            <label style="font-size:20px;color:red"><b> Price : @product.Price</b></label>                        
                        </div>
                        <a asp-action="Details" asp-controller="Home" asp-route-id="@product.Id" style="margin-left:120px" class="btn btn-info fa-pull-right ">Details </a>

                     </div>

                  </div>
            </div>
        </div>
    }
</div>
<div>
   @* @Html.PagedListPager(Model, page => Url.Content("~/Customer/Home/Index?page=" + page), PagedListRenderOptions.PageNumbersOnly)*@
    @Html.PagedListPager(Model, page => Url.Content("~/Customer/Home/Index?page=" + page), PagedListRenderOptions.MinimalWithItemCountText)
</div>




        @*@Html.PagedListPager(Model, page => Url.Content("~/Customer/Home/Index?page=" + page))*@
    

   @* @Html.PagedListPager(Model,generatePageUrl:c=>Href(contentPath: "~/Customer/Home/Index?page=" +));*@
